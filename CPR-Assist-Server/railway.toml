[build]
builder = "nixpacks"
buildCommand = "npm install && pip3 install requests --break-system-packages"

[deploy]
startCommand = "node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[nixpacks]
packages = ["nodejs", "python3", "python3Packages.pip"]