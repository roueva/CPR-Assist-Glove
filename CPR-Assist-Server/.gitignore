# Node.js dependencies
node_modules/

# Log files
*.log

# Environment variables
.env
.env.local

# Keep the directory but not the contents
!CPR-Assist-Server/data/.gitkeep

# Python cache
CPR-Assist-Server/scripts/__pycache__/
*.pyc

# Temporary files created during parsing
data/aed_greece_current.json

# Cache file (will be created on server via bootstrap)
data/parsed_availability_map.json

# Bootstrap request file (contains secret)
bootstrap-request.json


