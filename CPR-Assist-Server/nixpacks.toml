[phases.setup]
nixPkgs = [
  "nodejs",
  "python311",
  "python311Packages.pip"
]

[phases.install]
cmds = [
  "npm ci",
  "python3.11 -m pip install requests psycopg2-binary"
]

[start]
cmd = "node server.js"